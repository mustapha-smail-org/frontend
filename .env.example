# Base URL of the API Gateway that fronts the Catalog Service.
# Leave as the relative default in development so the Vite proxy handles CORS.
VITE_API_BASE_URL=/

# Optional: override the dev proxy target (defaults to http://localhost:8081).
VITE_DEV_API_PROXY_TARGET=http://localhost:8081

# Map tiles. The public OSM endpoint is NOT a production CDN (PRD FR-MAP-006);
# replace both values with a provider you are entitled to use before deploying.
VITE_MAP_TILE_URL=https://tile.openstreetmap.org/{z}/{x}/{y}.png
VITE_MAP_ATTRIBUTION=&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors
